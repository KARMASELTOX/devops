git init
git config --global user.email "shivagnr2004@gmail.com"
git remote add origin
git remote add origin https://github.com/KARMASELTOX/devops.git
git add .
git commit -m"version1"
git commit -m "intial commit"
git clone https://github.com/KARMASELTOX/devops.git
vi text.txt
git add .
git commit -m "shivag"
git status
 git push origin master
git log
git reset --hard ID

git checkout -b branch1
git add .
git commit -m "b1"
git switch master
git merge branch1
git diff master
git merge

FROM openjdk
WORKDIR /app
COPY . /app
RUN javac Add.java
CMD ["java","Add"]
docker build -t img1 .
docker run img1
